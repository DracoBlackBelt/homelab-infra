[defaults]
inventory = inventory/
host_key_checking = False
# Secrets live in vault-encrypted group_vars (e.g. vms/vault.yml). The password
# file sits at the repo root and is gitignored -- create it once per machine.
vault_password_file = ../.vault-pass
