[containers]
test-01 ansible_host=10.0.0.40 ansible_user=root

[vms]
test-vm01 ansible_host=10.0.0.41 ansible_user=debian

[vms:vars]
ansible_become=true

# setup.yml targets `test`. Kept to containers only so its behaviour is
# unchanged; add `vms` here to opt VMs into base setup (Docker, sshd hardening).
[test:children]
containers
