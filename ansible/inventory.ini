# Hosts come from tofu's vm_addresses output -- cloud-init sets these, so the
# tofu config is the source of truth, not a DHCP lease.
[vms]
test-vm01 ansible_host=10.0.0.41

[vms:vars]
ansible_user=debian
ansible_become=true
